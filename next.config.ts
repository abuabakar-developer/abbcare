import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true, // Enables better debugging
  swcMinify: true, // Minifies JavaScript for performance
};

export default nextConfig;


